<?xml version="1.0" encoding="UTF-8"?>
<!-- https://docs.liquibase.com/concepts/changelogs/xml-format.html -->
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
         http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <property  name="string.type"  value="varchar(255)"  dbms="!postgresql"/>
    <property  name="string.type"  value="text"  dbms="postgresql"/>

    <changeSet author="app" id="createTable-<%= tableName %>">
        <createSequence
                        sequenceName="<%= tableName %>_seq"
                        incrementBy="50"
                        startValue="1"
                        />
        <createTable tableName="<%= tableName %>">
            <column name="id" type="bigint" defaultValueSequenceNext="<%= tableName %>_seq">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <%_ entity.attributes.forEach((attr, index) => { _%>
            <%_ if (!attr.isId) { _%>
            <column name="<%= _.snakeCase(attr.name) %>" type="<%= getSqlType(attr.type, databaseType) %>">
                <constraints nullable="<%= !attr.required %>"<%= attr.unique ? ' unique="true"' : '' %>/>
            </column>
            <%_ } _%>
            <%_ }); _%>
            <%_ const manyToOneRelations = entity.relationships.filter(rel => rel.type === 'ManyToOne'); _%>
            <%_ manyToOneRelations.forEach((rel, index) => { _%>
            <column name="<%= _.snakeCase(rel.name) %>_id" type="bigint">
                <constraints nullable="<%= !rel.required %>"/>
            </column>
            <%_ }); _%>
        </createTable>
        <%_ if (manyToOneRelations.length > 0) { _%>
        <%_ manyToOneRelations.forEach((rel, index) => { _%>
        <addForeignKeyConstraint baseTableName="<%= tableName %>"
                                 baseColumnNames="<%= _.snakeCase(rel.name) %>_id"
                                 constraintName="fk_<%= tableName %>_<%= _.snakeCase(rel.targetEntity) %>"
                                 referencedTableName="<%= _.snakeCase(rel.targetEntity) %>"
                                 referencedColumnNames="id"/>
        <%_ }); _%>
        <%_ } _%>
    </changeSet>
</databaseChangeLog>
